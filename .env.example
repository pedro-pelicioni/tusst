# Copy to .env and fill in.
DATABASE_URL="postgresql://tusst:tusst@localhost:5432/tusst?schema=public"

# Auth.js (NextAuth v5). Generate a secret with: npx auth secret
AUTH_SECRET=""
AUTH_URL="http://localhost:3000"

# Local dev credentials login (any name -> logs you in). Only honored when
# NODE_ENV !== "production" — set to "true" for local dev.
AUTH_DEV_LOGIN="false"

# GitHub OAuth (https://github.com/settings/developers)
AUTH_GITHUB_ID=""
AUTH_GITHUB_SECRET=""

# Email magic-link (SMTP connection string, e.g. smtp://user:pass@host:587)
AUTH_EMAIL_SERVER=""
AUTH_EMAIL_FROM="login@tusst.dev"

# Submission grading. "docker" runs code for real in the hardened
# tusst-runner image — build it once: npm run runner:build
# "regex" (local dev without Docker) grades only the conceptual lessons
# (Stellar 101 / Soroban stubs); Rust lessons report "sandbox unavailable".
RUNNER_MODE="docker"

# GitHub OAuth (read-only: name + email). Create at github.com/settings/developers
#   Authorization callback URL: http://localhost:3000/api/auth/callback/github
# AUTH_GITHUB_ID / AUTH_GITHUB_SECRET above.

# Discord OAuth (read-only "identify email": display name + email).
# Create at discord.com/developers/applications → OAuth2
#   Redirect URL: http://localhost:3000/api/auth/callback/discord
AUTH_DISCORD_ID=""
AUTH_DISCORD_SECRET=""
