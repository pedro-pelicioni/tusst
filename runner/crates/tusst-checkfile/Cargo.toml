[package]
name = "tusst-checkfile"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true

# Authoring-time harness only. It never ships in the sandbox image — it exists
# so `npm run check:advanced` can evaluate a lesson's astChecks against its own
# referenceSolution on the host, with no docker and no network.

[dependencies]
serde_json.workspace = true
tusst-syntest = { path = "../tusst-syntest" }
